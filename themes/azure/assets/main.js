// Azure theme — behaviour for the generated blog.
// The Bloger runtime provides the topbar/sidebar shell, so no
// navigation wiring is needed here. Add theme behaviour below if
// you want any (e.g. reading progress, lightbox, analytics).
